def ascending_order(lst):
    sorted_lst=sorted(lst)
    return sorted_lst
def descending_order(lst):
    sorted_lst=sorted(lst,reverse=True)
    return sorted_lst
List1=[10,32,14,54,17,81]
print("Ascending order: ",ascending_order(List1))
print("Descending order: ",descending_order(List1))
